<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psx.server.mapper.AdminMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.psx.server.pojo.Admin">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password
    </sql>

    <insert id="insertAdmin" parameterType="com.psx.server.pojo.Admin"
            keyProperty="id" useGeneratedKeys="true">
        INSERT INTO admin(id,username,password)
        VALUES (#{id},#{username, jdbcType=VARCHAR},#{password, jdbcType=VARCHAR} )
    </insert>

</mapper>
